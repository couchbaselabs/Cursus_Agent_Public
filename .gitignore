# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
*.egg

# Virtual environment
venv/
.venv/
env/

# Playwright sessions (contain auth cookies)
.playwright_supportal/
.playwright_supportal_streamlit/
.playwright_netlog/

# Network capture logs (may contain session tokens)
network_log_*.json

# Data exports (customer / ticket data — never commit)
# Narrow on purpose: the repo legitimately tracks config + i18n JSON, so we
# ignore the known dump artifacts rather than a blanket *.json.
*.csv
tickets.json
sizer_import*.json
sfdc_graph_discovery.json
reports/
docs/reports/

# Debug / scratch outputs
scratch/

# Old scraper/ bootstrap dir (kept for historical lib refs only)
scraper/

# macOS
.DS_Store

# IDE
.vscode/
.idea/
*.swp
*.swo

# Secrets / env
.env
*.env

# Chainlit temp uploads
.files/

# Snapshot debug dumps
snap_debug/

# Generated PowerPoint / office lock files
docs/*.pptx
docs/~$*
